#include <bits/stdc++.h>

using namespace std;

int main(){
	vector<int> vec;
	vec.push_back(1);
	vec.push_back(2);
	vec.push_back(3);
	vec.push_back(4);
	vec.push_back(5);
	vec.push_back(6);
    cout << vec.size() << '\n';
    vec.clear();
    cout << vec.size() ;
	//for(auto v:first){
		//cout << v << '\t';
	//}
	return 0;
}